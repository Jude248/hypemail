// pages/index.js (Updated with all 4 plans)
import { useState } from 'react';

export default function Home() {
  const [user, setUser] = useState(null);

  return (
    <div className="container">
      <h1>AI Cold Email Bot</h1>
      
      <div className="plans">
        {/* Free Plan */}
        <div className="plan free">
          <h3>Free</h3>
          <p>5 emails/month</p>
          <button onClick={() => handleSignUp('free')}>Get Started</button>
        </div>

        {/* Basic Plan */}
        <div className="plan basic">
          <h3>Basic ($19/month)</h3>
          <p>50 emails/month</p>
          <button onClick={() => handleUpgrade('basic')}>Upgrade</button>
        </div>

        {/* Advanced Plan */}
        <div className="plan advanced">
          <h3>Advanced ($49/month)</h3>
          <p>200 emails/month</p>
          <button onClick={() => handleUpgrade('advanced')}>Upgrade</button>
        </div>

        {/* Elite Plan */}
        <div className="plan elite">
          <h3>Elite ($99/month)</h3>
          <p>Unlimited emails</p>
          <button onClick={() => handleUpgrade('elite')}>Upgrade</button>
        </div>
      </div>

      {/* Email Generator Component */}
      {user && <EmailGenerator user={user} />}
    </div>
  );
}
